name: CI - Distzilla

on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  perl-job:
    runs-on: ubuntu-latest
    steps:
      - uses: perlbot/action-dzil@v1.0.0
        env:
          EXTRA_DEPS: ""
